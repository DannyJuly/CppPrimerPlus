1. 没有动态内存分配，可以使用默认的赋值运算符
2. 定义为虚函数后才能在派生类中重新定义并且使用时，不会由于指针类型的问题导致函数调用错误
3.abstr_emp有派生类的共同特点，定义为虚基类满足ABC理念,并且再继承时不会出现基类调用两次
4.highfink使用其父类的数据
5.只输出abstr_tmp的数据，只需要定义一个operator就能满足
6.abstr_tmp不可以创建对象，但可以创建该类的指针
